#include <stdio.h>
int main() {
    int x,y,z;
    scanf("%d%d%d",&x,&y,&z);
    if(a+b>c||a+c>b||b+c>a){
        printf("Valid");
    }
    else{
        printf("Invalid");
    }
    return 0;
}